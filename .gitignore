# ── Node / Next (frontend) ──
node_modules/
.next/
out/
.turbo/
*.tsbuildinfo
next-env.d.ts
.env*.local

# ── Java / Maven (backend) ──
target/
*.class

# ── Secrets & configuration locale (NE PAS committer) ──
.env
keys/
*.pem
*.key

# ── Média / uploads stockés en local ──
uploads/

# ── Archives & fichiers temporaires ──
*.rar
*.zip
~$*

# ── OS / IDE ──
.DS_Store
Thumbs.db
.idea/
.vscode/
*.iml
*.log
